import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#53b0fd", // Color pulled from SponsorCX logo
    },
    secondary: {
      main: "#f2f2f2",
    },
  },
});

export default theme;
